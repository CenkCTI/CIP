import { render, screen } from "@testing-library/react";
import { afterEach, describe, expect, it, vi } from "vitest";
import { IocInbox } from "./ioc-inbox";
vi.mock("next/navigation",()=>({useRouter:()=>({refresh:vi.fn()})}));
vi.mock("@/app/osint/ioc-actions",()=>({acceptIocCandidate:vi.fn(),changeIocConnectionState:vi.fn(),connectOtx:vi.fn(),connectThreatFox:vi.fn(),disconnectOtx:vi.fn(),disconnectThreatFox:vi.fn(),ensureSyntheticIocConnection:vi.fn(),saveOtxSettings:vi.fn(),saveThreatFoxSettings:vi.fn(),syncIocProviderConnection:vi.fn(),triageIocCandidate:vi.fn()}));
const filters={view:"iocs" as const,ioc_q:"",ioc_status:"" as const,ioc_type:"" as const,ioc_provider:"",ioc_sort:"last" as const,ioc_min_confidence:"" as const,ioc_max_confidence:"" as const,ioc_port:"" as const,ioc_project:"",ioc_cursor:""};
afterEach(()=>vi.restoreAllMocks());
describe("manual IOC synchronization deployment",()=>{it("shows both accessible provider notes without polling or background requests",()=>{const fetchSpy=vi.spyOn(globalThis,"fetch"),timeoutSpy=vi.spyOn(globalThis,"setTimeout"),intervalSpy=vi.spyOn(globalThis,"setInterval");render(<IocInbox rows={[]} projects={[]} connections={[]} runs={[]} sources={{}} filters={filters} nextHref={null} syntheticEnabled={false}/>);const threatFox=screen.getByText("Automatic provider synchronization is not enabled in this deployment. Use Sync now to check for new observations.");const otx=screen.getByText("Manual synchronization only. Use Sync now to check subscribed Pulses.");expect(threatFox).toBeVisible();expect(otx).toBeVisible();expect(threatFox).toHaveAttribute("role","note");expect(otx).toHaveAttribute("role","note");expect(fetchSpy).not.toHaveBeenCalled();expect(timeoutSpy).not.toHaveBeenCalled();expect(intervalSpy).not.toHaveBeenCalled();expect(fetchSpy.mock.calls.flat().join(" ")).not.toContain("otx.alienvault.com")})});
