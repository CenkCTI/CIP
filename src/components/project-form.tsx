"use client";import { ActionForm, SubmitButton } from "@/components/form-status";import { priorities,researchTypes,type Project } from "@/lib/projects/schema";
export function ProjectForm({action,project}:{action:(s:{error?:string;success?:string},f:FormData)=>Promise<{error?:string;success?:string}>;project?:Partial<Project>}){return <ActionForm action={action}><label>Name<input className="field mt-1" name="name" required maxLength={120} defaultValue={project?.name}/></label><label>Description<textarea className="field mt-1" name="description" maxLength={2000} defaultValue={project?.description}/></label><div className="grid gap-4 md:grid-cols-2"><label>Research type<select className="field mt-1" name="research_type" defaultValue={project?.research_type??"CTI"}>{researchTypes.map(v=><option key={v}>{v}</option>)}</select></label><label>Priority<select className="field mt-1" name="priority" defaultValue={project?.priority??"MEDIUM"}>{priorities.map(v=><option key={v}>{v}</option>)}</select></label></div><label>Tags<input className="field mt-1" name="tags" placeholder="apt, ransomware" defaultValue={project?.tags?.join(", ")}/></label><SubmitButton>{project?.id?"Save project":"Create project"}</SubmitButton></ActionForm>}
