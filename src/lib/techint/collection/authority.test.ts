import { describe,expect,it } from 'vitest';import { assertLegacyCollectionAllowed,collectionAuthorityEnforced,legacyCollectionAllowed,LEGACY_COLLECTION_BLOCKED_MESSAGE } from './authority';
describe('NODE-2G collection authority guard',()=>{it.each(['CISA_KEV','NVD_CVE','FIRST_EPSS','THREATFOX','MALWAREBAZAAR'])('blocks every Node-authoritative source by default: %s',source=>{expect(legacyCollectionAllowed(source,{})).toBe(false);expect(()=>assertLegacyCollectionAllowed(source,{})).toThrow(LEGACY_COLLECTION_BLOCKED_MESSAGE);});it.each(['enable','resume','sync now','collector enable','collector rotate','manual claim','due claim','collector tick','work claim'])('enforces the trusted execution boundary for %s',()=>expect(collectionAuthorityEnforced({NODE_ENV:'production'})).toBe(true));it('allows pause, archive, and inert settings by not applying a start-operation guard',()=>expect(legacyCollectionAllowed('NON_NODE_SOURCE',{})).toBe(true));it('requires deliberate manual rollback mode',()=>{const env={CITEM_LEGACY_TECHINT_COLLECTION_MODE:'MANUAL_ROLLBACK'};expect(legacyCollectionAllowed('NVD_CVE',env)).toBe(true);expect(collectionAuthorityEnforced(env)).toBe(false);});it('lets ordinary tests opt out but permits explicit NODE_AUTHORITY regression tests',()=>{expect(collectionAuthorityEnforced({NODE_ENV:'test'})).toBe(false);expect(collectionAuthorityEnforced({NODE_ENV:'test',CITEM_LEGACY_TECHINT_COLLECTION_MODE:'NODE_AUTHORITY'})).toBe(true);});});
