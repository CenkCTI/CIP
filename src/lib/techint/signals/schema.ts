import { z } from "zod";
import { entityKinds,entityRoles,recordingAssertionBases,signalLifecycles,signalSeverities,signalTypes,sourceFamilies } from "./types";
import { indicatorCanonicalKey,validateCanonicalKey,validateSourceDefinedCanonicalKey } from "./canonical-key";
const jsonBound=(max:number)=>z.record(z.string(),z.unknown()).refine(v=>Buffer.byteLength(JSON.stringify(v))<=max,`JSON must be at most ${max} bytes.`);
const instant=z.iso.datetime({offset:true});
const safeUrl=z.url().max(2048).refine(raw=>{const u=new URL(raw);return ["http:","https:"].includes(u.protocol)&&!u.username&&!u.password;},"Only credential-free HTTP(S) URLs are accepted.");
const identifier=z.string().trim().min(1).max(200).regex(/^[^\u0000-\u001f]+$/);
export const signalEntityAssertionSchema=z.object({entityKind:z.enum(entityKinds),displayValue:z.string().trim().min(1).max(500),normalizedValue:z.string().trim().min(1).max(500),semanticRole:z.enum(entityRoles),assertionBasis:z.enum(recordingAssertionBases),confidence:z.number().int().min(0).max(100).nullable().optional(),indicatorType:z.enum(["IP","CIDR","DOMAIN","URL","HASH","EMAIL"]).nullable().optional(),sourceEntityType:z.string().trim().max(80).nullable().optional(),sourceEntityId:z.uuid().nullable().optional()}).strict().superRefine((v,ctx)=>{if((v.entityKind==="INDICATOR")!==Boolean(v.indicatorType))ctx.addIssue({code:"custom",path:["indicatorType"],message:"Indicator subtype mismatch."});if((v.sourceEntityType==null)!==(v.sourceEntityId==null))ctx.addIssue({code:"custom",path:["sourceEntityId"],message:"Source entity snapshots must be paired."});if(v.entityKind==="INDICATOR"&&v.indicatorType)try{if(indicatorCanonicalKey(v.indicatorType,v.normalizedValue)!==`indicator:${v.indicatorType}:${v.normalizedValue}`)throw new Error();}catch{ctx.addIssue({code:"custom",path:["normalizedValue"],message:"Indicator must be canonical."});}});
export const recordTechnicalSignalSchema=z.object({actorId:z.uuid(),signal:z.object({signalType:z.enum(signalTypes),canonicalKey:z.string().trim().min(1).max(700),title:z.string().trim().min(1).max(500),summary:z.string().max(4000),lifecycle:z.enum(signalLifecycles),severity:z.enum(signalSeverities),confidence:z.number().int().min(0).max(100).nullable(),facts:jsonBound(65536),publishedAt:instant.nullable(),observedAt:instant.nullable(),effectiveAt:instant,supersededBySignalId:z.uuid().nullable().optional()}).strict(),observation:z.object({sourceFamily:z.enum(sourceFamilies),sourceSystem:identifier,sourceRecordKey:identifier,sourceRevisionKey:identifier.nullable().optional(),sourceUrl:safeUrl.nullable().optional(),sourceTitle:z.string().trim().max(500).nullable().optional(),sourcePublishedAt:instant.nullable(),sourceModifiedAt:instant.nullable(),sourceObservedAt:instant.nullable(),receivedAt:instant,effectiveAt:instant,sourceSnapshot:jsonBound(65536)}).strict(),entityAssertions:z.array(signalEntityAssertionSchema).max(100)}).strict().superRefine((v,ctx)=>{if(v.signal.effectiveAt!==v.observation.effectiveAt)ctx.addIssue({code:"custom",path:["observation","effectiveAt"],message:"Effective times must match exactly."});try{validateCanonicalKey(v.signal.signalType,v.signal.canonicalKey);if(/^(report|advisory):/.test(v.signal.canonicalKey))validateSourceDefinedCanonicalKey(v.signal.canonicalKey,v.observation.sourceSystem,v.observation.sourceRecordKey);}catch(e){ctx.addIssue({code:"custom",path:["signal","canonicalKey"],message:e instanceof Error?e.message:"Invalid key."});}});
export type RecordTechnicalSignalInput=z.infer<typeof recordTechnicalSignalSchema>;
export const recordTechnicalSignalResultSchema=z.object({signal_id:z.uuid(),observation_id:z.uuid(),revision_id:z.uuid().nullable(),signal_created:z.boolean(),observation_created:z.boolean(),revision_created:z.boolean(),duplicate_observation:z.boolean(),disposition:z.enum(["CURRENT","SUPPORTING","STALE","CONFLICTING"]),current_revision_number:z.number().int().positive(),entity_assertions_created:z.number().int().nonnegative()}).strict();
export type RecordTechnicalSignalResult=z.infer<typeof recordTechnicalSignalResultSchema>;
