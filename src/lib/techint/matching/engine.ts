export const PROFILE_MATCH_ENGINE_VERSION="2.3E-v1" as const;
export type MatchEvidence={kind:string;value:string;basis:"CANONICAL"|"ASSERTION"|"CONTEXT";profileItemId?:string;entityId?:string;assertionId?:string};
export function evaluateProfileMatch(evidence:MatchEvidence[],pendingIdentityCount=0){if(!evidence.length)return null;const direct=evidence.filter(x=>x.basis!=="CONTEXT");const canonical=direct.filter(x=>x.basis==="CANONICAL");const matchQuality=canonical.length?"CONFIRMED":direct.length?"PROVISIONAL":"CONTEXTUAL";const internalScore=Math.min(100,canonical.length*50+direct.filter(x=>x.basis==="ASSERTION").length*40+evidence.filter(x=>x.basis==="CONTEXT").length*15);const relevance=internalScore>=40?"HIGH":internalScore>=20?"MEDIUM":"LOW";return{relevance,matchQuality,internalScore,reasonCodes:evidence.map(x=>`${x.basis==="ASSERTION"?"SOURCE_ASSERTION":x.basis}_MATCH_${x.kind.toUpperCase()}`),pendingIdentityCount,engineVersion:PROFILE_MATCH_ENGINE_VERSION}}
