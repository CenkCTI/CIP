import "server-only";
import type {IocProviderAdapter} from "../../types";
import {fetchThreatFoxIocs,testThreatFoxConnection} from "./client";
import {ThreatFoxError} from "./errors";
import {mapThreatFoxItem} from "./mapping";
export const threatFoxAdapter:IocProviderAdapter={key:"THREATFOX",displayName:"ThreatFox Community API",credentialRequired:true,supportedTypes:["DOMAIN","IPV4","IPV6","URL"],supportsScheduling:true,testConnection:testThreatFoxConnection,async sync({credential,settings,signal}){if(!credential)throw new ThreatFoxError("THREATFOX_CREDENTIAL_REQUIRED");const raw=await fetchThreatFoxIocs(credential,Number(settings.lookback_days??1),signal);if(!raw||typeof raw!=="object"||Array.isArray(raw))throw new ThreatFoxError("THREATFOX_INVALID_RESPONSE");const response=raw as {query_status?:unknown;data?:unknown};if(typeof response.query_status!=="string"||response.query_status.length>100)throw new ThreatFoxError("THREATFOX_INVALID_RESPONSE");if(response.query_status!=="ok")throw new ThreatFoxError("THREATFOX_QUERY_FAILED");if(!Array.isArray(response.data))throw new ThreatFoxError("THREATFOX_INVALID_RESPONSE");if(response.data.length>1000)throw new ThreatFoxError("THREATFOX_ITEM_LIMIT");const items=[];for(const rawItem of response.data){try{items.push(mapThreatFoxItem(rawItem))}catch{continue}}const first=items.map(x=>x.first_seen_at).filter(Boolean).sort().at(-1)??null;const ids=items.map(x=>Number(x.provider_item_id)).filter(Number.isFinite);return{status:"SUCCEEDED",items,nextCursor:JSON.stringify({schema_version:1,max_id:ids.length?Math.max(...ids):null,max_first_seen:first})};}};
