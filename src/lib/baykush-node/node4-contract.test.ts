import {describe,expect,it} from 'vitest';
import {changeSchema,comparisonSchema,sourceStatusSchema} from './schemas';

describe('NODE-4 client contracts',()=>{it('accepts selected-range status independently from health',()=>expect(sourceStatusSchema.parse({sourceKey:'NVD_CVE',displayName:'NVD',authority:'BAYKUSH_INTELLIGENCE_NODE',operationalHealth:'HEALTHY',lastAttemptAt:null,lastSuccessfulCollectionAt:null,freshness:'FRESH',coverage:'PARTIAL',dataAvailability:'PARTIAL',historicalBackfillStatus:'RUNNING'})).toMatchObject({operationalHealth:'HEALTHY',coverage:'PARTIAL'}));it('accepts Node-owned unavailable comparisons without local arithmetic',()=>expect(comparisonSchema.parse({measurementKey:'exploitation.epss.scored_records',current:{from:'2026-08-12T00:00:00.000Z',to:'2026-08-13T00:00:00.000Z',value:null},previous:{from:'2026-08-11T00:00:00.000Z',to:'2026-08-12T00:00:00.000Z',value:null},absoluteDelta:null,percentChange:null,comparisonStatus:'INCOMPATIBLE_SOURCE_MODEL',reason:'SOURCE_MODEL_OR_POPULATION_PROFILE_CHANGED'}).comparisonStatus).toBe('INCOMPATIBLE_SOURCE_MODEL'));it('bounds change responses at 100 rows',()=>expect(()=>changeSchema.array().max(100).parse(Array.from({length:101},()=>({measurementKey:'x'})))).toThrow());});
