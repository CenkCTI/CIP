import { requireUser } from "@/lib/auth";
import { getNodeProvenance } from "@/lib/baykush-node/queries";

export default async function Page({params,searchParams}:{params:Promise<{revisionId:string}>;searchParams:Promise<Record<string,string|undefined>>}){await requireUser();const [{revisionId},query]=await Promise.all([params,searchParams]);const response=await getNodeProvenance(revisionId);const data=response.data;return <section className="space-y-5"><header className="citem-page-header"><div><p className="citem-eyebrow">BAYKUSH Intelligence Node / bounded provenance</p><h1 className="citem-title">Measurement bucket provenance</h1><p className="citem-subtitle">Canonical provenance metadata only; complete provider payloads are not redistributed.</p></div></header><article className="card panel-corners space-y-2 text-sm text-stone-400"><p>Measurement: {query.measurementKey??data.measurement.key}</p><p>Bucket: {query.bucketStart??"—"} → {query.bucketEnd??"—"}</p><p>Value: {query.value===''||query.value===undefined?"Unavailable / gap":query.value}</p><p>Coverage: {query.coverage??"UNKNOWN"} · availability: {query.availability??"UNKNOWN"}</p><p>Revision: {data.revision.revisionNumber} ({data.revision.id})</p><p>Contract/calculation: {String(data.measurement.contractVersion??"—")} / {String(data.measurement.calculationVersion??"—")}</p><p>Represents: {String(data.measurement.represents??"—")}</p><p>Does not represent: {String(data.measurement.doesNotRepresent??"—")}</p></article><section className="card panel-corners"><h2 className="citem-section-title">Bounded provenance inputs</h2><pre className="mt-3 overflow-auto whitespace-pre-wrap text-xs text-stone-400">{JSON.stringify(data.inputs,null,2)}</pre></section></section>}
