import { createStandaloneIntelProfile } from "@/app/techint/actions";import { IntelProfileForm } from "@/components/techint/profile-form";export default function Page(){return <section className="space-y-5"><header className="citem-page-header"><div><p className="citem-eyebrow">TechINT / Profiles</p><h1 className="citem-title">Create standalone profile</h1><p className="citem-subtitle">Standalone profiles are independent from Investigations.</p></div></header><IntelProfileForm action={createStandaloneIntelProfile}/></section>}
