/* eslint-disable @typescript-eslint/no-explicit-any */
import Link from "next/link";import { requireUser } from "@/lib/auth";import { listInvestigationIntelProfiles } from "@/lib/techint/queries";
export default async function Page(){const {supabase}=await requireUser();const {data,error}=await listInvestigationIntelProfiles(supabase);const rows=(data??[]) as any[];return <section className="space-y-5"><header className="citem-page-header"><div><p className="citem-eyebrow">TechINT / InvestINT</p><h1 className="citem-title">Investigation-linked INT Profiles</h1><p className="citem-subtitle">Central index of INT Profiles created inside existing Investigations. Standalone profiles are excluded.</p></div></header>{error?<div className="card text-red-300">Unable to load InvestINT. Apply migration 031.</div>:!rows.length?<div className="citem-empty panel-corners"><h2 className="citem-section-title">No Investigation INT Profiles</h2><p className="mt-2 text-sm text-stone-500">Open an Investigation and create its Intel Profile. Technical matches will be introduced in later phases.</p></div>:<div className="grid gap-3">{rows.map(r=><div className="card" key={r.id}><h2 className="citem-section-title">{r.projects?.name??"Investigation"}</h2><p className="mt-1 text-sm text-stone-400">{r.name} · {r.status} · {r.priority} · Updated {new Date(r.updated_at).toLocaleString()}</p><p className="mt-2 text-xs text-stone-500">{r.intel_profile_items?.[0]?.count??0} profile items. No match counts are shown in Phase 2.3A.</p><div className="mt-3 flex flex-wrap gap-2"><Link className="citem-button-ghost" href={`/techint/investint/${r.project_id}`}>Open profile summary</Link><Link className="citem-button-ghost" href={`/projects/${r.project_id}`}>Open Investigation</Link><Link className="citem-button" href={`/projects/${r.project_id}/intel-profile`}>Open Investigation Intel Profile</Link></div></div>)}</div>}</section>}
