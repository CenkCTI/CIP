import { notFound } from "next/navigation";import { updateProject,deleteProject } from "@/app/actions";import { ProjectForm } from "@/components/project-form";import { requireUser } from "@/lib/auth";import type { Project } from "@/lib/projects/schema";
export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params;const {supabase}=await requireUser();const {data,error}=await supabase.from("projects").select("*").eq("id",id).single<Project>();if(error||!data)notFound();const update=updateProject.bind(null,id);const del=deleteProject.bind(null,id,data.name);return <section className="mx-auto max-w-4xl"><h1 className="text-3xl font-bold text-white">{data.name}</h1><div className="mt-6 border-b border-slate-800"><button className="border-b-2 border-cyan-300 px-4 py-2">Overview</button><span className="px-4 py-2 text-slate-500" aria-disabled="true">Evidence · coming soon</span><span className="px-4 py-2 text-slate-500" aria-disabled="true">Reports · coming soon</span></div><div className="mt-6 grid gap-6 lg:grid-cols-[1fr_320px]"><div className="card"><h2 className="mb-4 font-semibold text-white">Edit project</h2><ProjectForm action={update} project={data}/></div><div className="card"><h2 className="font-semibold text-red-200">Delete project</h2><p className="mt-2 text-sm text-slate-400">Type the project name to confirm: {data.name}</p><form action={del} className="mt-4 space-y-3"><input className="field" name="confirm"/><button className="rounded-lg bg-red-500 px-4 py-2 font-semibold text-white">Delete</button></form></div></div></section>}
