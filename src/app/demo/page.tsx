import Link from "next/link"; import { demoBanner, demoData } from "@/lib/demo/fixture";
export default function DemoPage(){return <main className="min-h-screen p-8"><div className="mx-auto max-w-6xl space-y-6"><div className="rounded border border-amber-700 bg-amber-950/40 p-3 text-amber-100">{demoBanner}</div><header className="flex flex-wrap items-center justify-between gap-3"><div><p className="text-sm uppercase tracking-[.3em] text-cyan-300">No-account demo</p><h1 className="text-3xl font-bold text-white">{demoData.project.name}</h1><p className="text-slate-300">Repository-owned deterministic fixture. No Supabase project query, uploads, or persistence.</p></div><div className="flex gap-2"><Link className="rounded bg-cyan-400 px-4 py-2 font-semibold text-slate-950" href="/demo/ai">Try Demo AI</Link><Link className="rounded border border-slate-700 px-4 py-2" href="/auth/sign-up">Create Account to Save</Link></div></header><section className="grid gap-4 md:grid-cols-4">{Object.entries(demoData.dashboard).map(([k,v])=><div key={k} className="card"><div className="text-2xl font-bold text-white">{v}</div><div className="capitalize text-slate-400">{k}</div></div>)}</section><section className="grid gap-4 lg:grid-cols-2"><div className="card"><h2 className="font-semibold text-white">Research Notes</h2>{demoData.notes.map(n=><details key={n.id} className="mt-3 rounded border border-slate-800 p-3"><summary>{n.title}</summary><p className="mt-2 text-sm text-slate-300">{n.body}</p></details>)}</div><div className="card"><h2 className="font-semibold text-white">Evidence Metadata</h2>{demoData.evidence.map(e=><p key={e.id} className="mt-2 text-sm"><strong>{e.title}</strong> — {e.type}; {e.storage}</p>)}</div><div className="card"><h2 className="font-semibold text-white">Timeline & Tasks</h2>{demoData.timeline.map(t=><p key={t.id} className="text-sm">{t.date}: {t.title}</p>)}<hr className="my-3 border-slate-800" />{demoData.tasks.map(t=><p key={t.id} className="text-sm">{t.title} — {t.status}</p>)}</div><div className="card"><h2 className="font-semibold text-white">CTI, Graph, Report Preview</h2><p className="text-sm">Fictional actor: {demoData.cti.actors[0].name}. Malware: {demoData.cti.malware[0].name}. Indicators: {demoData.cti.indicators.join(", ")}.</p><p className="mt-2 text-sm">Graph nodes: {demoData.graph.nodes.join(" → ")}</p><p className="mt-2 text-sm">Report: {demoData.report.title} ({demoData.report.sections.join(", ")})</p><button className="mt-4 rounded border border-slate-700 px-3 py-2 text-sm" type="button">Demo actions are view-only; create an account to persist changes.</button></div></section></div></main>}
