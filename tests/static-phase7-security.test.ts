import { describe, expect, it } from "vitest"; import { readFileSync } from "node:fs"; import { execSync } from "node:child_process";
describe("Phase 7 static security",()=>{ it("keeps server secrets out of NEXT_PUBLIC",()=>{ const env=readFileSync(".env.example","utf8"); expect(env).not.toMatch(/NEXT_PUBLIC_.*(SERVICE_ROLE|BYOK_COOKIE|TURNSTILE_SECRET|PROVIDER.*KEY)/); }); it("does not add api key columns",()=>{ const files=execSync("rg -l 'api_key|apikey|secret_key' supabase/migrations src || true").toString().trim().split(/\n/).filter(Boolean); for (const f of files) expect(readFileSync(f,"utf8")).not.toMatch(/create table[\s\S]*(api_key|apikey)/i); }); });
