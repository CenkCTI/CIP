import {describe,expect,it} from "vitest"; import {readFileSync} from "node:fs"; import {createVersionSchema,productTypes} from "@/lib/reports/versioning";
describe("Phase 2.1F",()=>{it("defines all intelligence product types",()=>expect(productTypes).toHaveLength(8));it("requires complete analyst assessment metadata",()=>expect(createVersionSchema.safeParse({}).success).toBe(false));it("contains database integrity controls",()=>{const sql=readFileSync("supabase/migrations/202607300022_phase2_1f_intelligence_products_versioning.sql","utf8");for(const token of ["report_versions","report_version_references_exactly_one","guard_report_versions","guard_published_report_delete","publish_report_version","enable row level security"])expect(sql).toContain(token)})});
