import{describe,expect,it}from"vitest";import{decodeCursor,encodeCursor,feedInputSchema,filterSchema,triageSchema}from"@/lib/osint/schema";describe("OSINT bounded inputs",()=>{it("round trips keyset cursors and rejects malformed cursors",()=>{const c=encodeCursor("2026-07-31T00:00:00.000Z","10000000-0000-4000-8000-000000000001");expect(decodeCursor(c)?.id).toBe("10000000-0000-4000-8000-000000000001");expect(decodeCursor("bad")).toBeNull()});it("validates modes, dates and ranges",()=>{expect(filterSchema.safeParse({mode:"saved",from:"2026-01-01",to:"2026-01-31"}).success).toBe(true);expect(filterSchema.safeParse({mode:"bad"}).success).toBe(false);expect(filterSchema.safeParse({from:"2026-02-01",to:"2026-01-01"}).success).toBe(false)});it("bounds feed intervals and triage actions",()=>{expect(feedInputSchema.safeParse({name:"x",description:"",url:"https://example.com",enabled:true,schedulerEnabled:true,interval:15}).success).toBe(true);expect(feedInputSchema.safeParse({name:"x",description:"",enabled:true,schedulerEnabled:true,interval:1}).success).toBe(false);expect(triageSchema.safeParse({itemId:"10000000-0000-4000-8000-000000000001",action:"dismiss"}).success).toBe(true)})});
