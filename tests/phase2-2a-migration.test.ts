import {readFileSync} from "node:fs";import {describe,expect,it} from "vitest";
const sql=readFileSync("supabase/migrations/202607300023_phase2_2a_research_feed_ingestion.sql","utf8");
describe("migration 023 contract",()=>{it.each(["research_feed_sources","research_feed_fetch_runs","research_items","research_item_fingerprints","research_feed_item_observations"])("creates %s",t=>expect(sql).toContain(`create table public.${t}`));it("has leases and transactional RPCs",()=>{expect(sql).toContain("fetch_lease_expires_at");expect(sql).toContain("claim_research_feed_fetch");expect(sql).toContain("complete_research_feed_fetch")});it("enables RLS on every table",()=>expect(sql.match(/enable row level security/g)).toHaveLength(5));it("enforces fingerprint uniqueness",()=>expect(sql).toContain("unique(project_id,fingerprint_type,fingerprint_hash)"));});
